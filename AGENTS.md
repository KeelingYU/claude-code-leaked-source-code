# Claude Code 参考仓库 AI 分析规则

## 目标
本仓库仅用于参考分析、功能拆解、架构学习与安全研究，不作为直接运行、直接二次开发或自动修复的工作目录。
其中 `src/` 目录视为原始参考源码目录，必须严格只读。
  仓库根目录下以及 `src/` 之外的文件，视为分析输出区域，可用于新增、修改、删除、重命名分析文档、说明文件、忽略规则
  等辅助内容。

## 强制规则

1. `src/` 目录及其所有子目录、文件，只能只读分析：
     - 不得修改
     - 不得删除
     - 不得重命名
     - 不得移动
2. 允许在 `src/` 之外新增或修改辅助文件，但必须满足：
     - 不得与 `src/` 内源码混淆
     - 不得伪装成原始源码的一部分
     - 必须明显属于分析、说明、整理、发布辅助用途
3. 所有新增文件默认只能放在 `src/` 之外。
4. 不得在本仓库内执行会改变系统状态或仓库内容的操作，包括但不限于：
   - 安装依赖
   - 启动服务
   - 执行构建
   - 执行测试
   - 执行登录
   - 执行 OAuth 流程
   - 安装插件
   - 安装 MCP server
   - 修改、删除、重命名仓库源码
5. 不得从本仓库分析任务中读取以下路径，除非用户明确单次授权：
   - `~/.ssh`
   - `~/.claude`
   - `~/.codex`
   - `~/.aws`
   - `~/.config`
   - 任意 `.env` 文件
   - 任意凭据、token、key、cookie、浏览器 profile 目录
6. 不得把本仓库内容与用户其他私有项目目录混合分析。
7. 不得基于本仓库直接生成“逐文件照抄式”实现；只允许提炼：
   - 功能模块
   - 数据流
   - 权限模型
   - 架构模式
   - 安全边界
8. 若用户要求继续开发，请在**新的独立仓库**中完成，且默认不复用原文件内容。但在 `src/` 之外新增 `.gitignore`、`README.md`、分析文档、发布说明等仓库级辅助文件，属于允许范围。

## 允许的操作

- 阅读源码
- 搜索符号、目录和调用关系
- 归纳功能模块
- 输出架构图、时序图、权限模型
- 输出威胁模型、安全建议、重开发方案
- 在本仓库根目录新增分析文档

## 推荐工作方式

1. 先做模块拆解，不直接写代码。
2. 先提炼接口和职责，再设计新实现。
3. 任何需要运行、联网、装依赖、接真实账号的步骤，都应转移到隔离环境或新仓库中进行。

## 输出要求

- 默认给出高层结构，不直接复刻原实现。
- 如果发现高风险能力，优先提示：
  - 本地文件读写
  - 命令执行
  - 外网访问
  - 插件扩展
  - MCP 集成
  - 同步与遥测
